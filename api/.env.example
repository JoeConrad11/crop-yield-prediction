SUPABASE_URL=https://your-project.supabase.co
# anon (public/RLS-scoped) key -- NOT the service-role key used by
# src/supabase_writer.py for the Modal cron write path. Find it in the
# Supabase dashboard under Settings -> API -> Project API keys -> anon/public.
SUPABASE_ANON_KEY=your-anon-key
