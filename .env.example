NASS_API_KEY=
GEE_PROJECT_ID=
NOAA_CDO_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# GEE_SERVICE_ACCOUNT_JSON is only needed for headless environments (e.g. Modal),
# not local dev -- local dev uses `earthengine authenticate` instead. See
# src/gee_auth.py and src/modal_app.py for how it's used.
GEE_SERVICE_ACCOUNT_JSON=
